/* Purpose: CLI entry for the MeTTaScript-backed server; the library is
 * mettascript_space_server.ts and this file only invokes it. */
import { runCli } from "./mettascript_space_server.ts";

runCli();
